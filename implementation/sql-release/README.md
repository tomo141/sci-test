# 検証済みの本番SQL

2026-09-15。本人のSQL実行とGitHub `tomo141/sci-test` への明示承認を受け、ローカルで検証済みの固定ファイルを同じ内容で保存した。バックアップの行データ・会員情報・認証情報は含まない。元のマイグレーションは `supabase/migrations/`。

1. `additive-schema.sql`：0004〜0019の追加変更（0007を除く）。初回だけ実行する。
2. `followup-schema-0020-0028.sql`：前段のハッシュ台帳を確認した後の追加変更。
3. `followup-schema-0029-0029.sql`：本番適用済みの24件をハッシュで確認し、MyASP同期の0029だけを追加する。
4. `security-cutover-after-0029.sql`：改装アプリへの切替時に実行する0007。前段25件の適用が必要。先行実行しない。

`security-cutover-after-0028.sql` は以前の準備記録として保持する。現在の切替には0029後のファイルを使う。適用済みの追加SQLをまとめて再実行しない。差分の再生成は `node scripts/prepare-science-upgrade.mjs 28` でローカルに出力できる。

各ファイルはトランザクションと再実行防止を持つ。エラー・タイムアウト時は再実行前に台帳と実データを確認する。ファイル内の最後のSELECTは検証用であり、表示されたことだけを成功とみなさない。

`manifest.json` のSHA-256はローカル検証版の値。ブラウザへ転記する場合は、エディターの全選択・コピーで読み戻した全文との一致を実行前に確認する。アクセシビリティ表示の省略箇所だけで一致を判断しない。実行後はRESTでハッシュ台帳と旧データ保持を照合する。

実行状況は [本番反映記録](../DEPLOYMENT.md) を参照。

## 2026-09-16の適用済み追加

- `followup-schema-0030-0031.sql`：投稿条件の採択記録と既出問題のフォールバック。
- `followup-schema-0032.sql`：70%対応の採点版を受験開始時に固定。
- `followup-schema-0033.sql`：作問の対象段階と、分野別の任意自己評価を非公開・版付きで保存。SHA-256 `6c04b45d7ec0f55b58f7313b9a5d10051a8ddaa182a23111d2851d8b21bfae49`。

上記は適用済み。繰り返して実行しない。0033の本番適用はGitHubの不変コミット `deec3c9` のファイル表示本文とローカル検証版の全文ハッシュを照合し、空のSQLエディターへ入力した。実行後の台帳・保存権限・既存データと実APIを検証済み。[7段階の採択と検証](../KNOWLEDGE-LEVELS-ADOPTED-20260916.md)。
