# 回答画面の復元・案内ページ・数式表示

出典：このチャットの本人の依頼と選択回答。観測日・記録日：2026-09-17（Asia/Tokyo）。

## 採択された表示と操作

- 大改装前の問題カード、大きな問題文、PCの2列選択肢、進捗・正答数・途中スコアの構成を復元する。
- クリック・タップは即回答。数字の1〜4は初期状態では選択だけ。「キーボードの1,2,3,4で即回答」をチェックした場合だけ、数字キーでも確定する。
- 回答後も問題と選択肢を残し、正誤・正解・短い解説を直後に表示。詳しい解説と出典は開閉する。次の問題には明示操作で進む。スペースキーによる次への移動を復元し、入力欄・ボタン等の操作を妨げない。
- 良問・改善報告は現行の保存先へ送る。ログイン済みの本人が回答を確定した問題は、その場で復習用に保存できる。旧APIや端末だけの架空保存は復活させない。

## スコアに関する本人の選択

- 総合は**最初の1問の回答後から**表示。今回の採点対象の回答だけで推定し、未測定分野には既存モデルの事前分布による仮推定を含める。未測定分野数と推定幅を併記する。
- 分野単独の受験は**1〜99点**で表示し、現行の結果画面と揃える。総合は10〜990点。
- 本画面用の暫定表示であり、保存済みの最終結果や採点式は変更しない。10分野が測定済みになると、途中表示の点と幅は同じ回答に対する結果計算と一致する。
- 今週の10問・ラボは従来どおり正答数を表示し、実力スコアを付けない。除外・未回答の問題は採点に混ぜない。

## 全問題の暫定レベル（本人承認済み）

| 回答画面の表示 | 70％基準の難度D70 |
|---|---:|
| レベル1 | 150未満 |
| レベル2 | 150以上300未満 |
| レベル3 | 300以上450未満 |
| レベル4 | 450以上600未満 |
| レベル5 | 600以上750未満 |
| レベル6 | 750以上900未満 |
| レベル7 | 900以上 |

表示規約 `question-level-p70-v1-20260917`。出題時に保存されたa・b・cからD70を換算するので、開始済みの問題の難度を後のバンク変更で上書きしない。問題画面には「レベルN」のみを表示する。作問・自己評価の7種類の人物像との対応を検証済みと扱わず、得点計算にも使わない。分野スコア1〜99点の7分割ではない。

## 追加依頼

- `/about`を初めて訪れた人向けに全面改稿。検定の概要、10分野、受け方、解説、スコア、共有、続きの受験、ラボを説明し、内部の採択経緯や計算実装を本文から外す。
- `/lab`に共通の認証対応ヘッダー、PC・スマホのグローバルナビゲーションとフッターを付ける。
- `r⃗`が欠字になる問題を修正。矢印・上線・点をフォントに依存しない表示にし、回答・解説・結果の復習・保存した復習・運営確認に共通適用。問題データ、正解、採点、出題履歴は変更しない。

## 検証状況

ローカル実装済み。単体・DB検査219件、全体lint・型検査・本番用ビルドが通過。PC・スマホのブラウザ34件が通過（独立した接続DBを要する2件は対象外）。初回は進捗に追加した割合表示により完全一致のテスト2件が不一致となり、照合対象を修正して該当2件を再実行し通過。数式・旧レイアウト・案内本文を画像でも確認した。本番反映は未実施。本変更のためのSQL・新規契約・案内メール配信は不要。
