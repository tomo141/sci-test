# 全分野科学検定：公開済み範囲と追加反映

**現在地（2026-09-16 14:57 JST記録）**：採点修正 `472bcfa` を既存本番へ反映済み。Vercel `9kAJ8NdP5HipgorqG9hm56o76AcZ` は14:46:59 JST Ready。SQL0032を適用し、70%対応の `science-3pl-p70-linear-v2` を有効化した。公開URLは https://sci-test.rikei-talk.com/ 。

全197テスト、PC・スマホのブラウザ24件、全体lint・型検査・本番用ビルドが通過（DB接続を前提とするローカル2件は対象外）。本番の匿名20問受験を完走し、正誤・解説、数字キーの初期選択、再読込、10分野の無加重合計、990点上限と新採点版を確認。検証用受験だけを除去し、実受験2件・回答20件・出題21件・旧版の1,000問パラメーターと設定はバックアップと一致。

S(θ)=500+200θ、N(0,2²)、グリッド−10〜10は実装上の初期規約。50/100問で990へ到達する回答経路を審査済みバンクで確認したが、実回答による精度保証や学位との対応付けは未完了。旧受験・結果は旧採点版を保持し、新規受験に新採点版を使用する。

人物像19段階は比較案として保存し、[作問・自己評価7種類、公開説明5〜6段階の提案](SCORE-BANDS-AND-TOEIC-20260916.md)は未採択・未公開。毎時集計は稼働を観測済み。MyASPは接続確認HTTP 200だが、専用12項目の定義がAPIに返らず同期は停止中。案内の実配信も無効。

検証記録：[本番状態](checks/p70-production-schema-20260916.json)、[旧データの照合](checks/p70-preservation-20260916.json)、[本番の受験確認](checks/p70-production-smoke-20260916.json)。

以下は各時点の実装・反映の履歴。


観測・記録日：2026-09-16。担当はこのタスクのAstra / gpt-6-astra / max。事業全体の正本は変更していない。

既存本番の改装公開と、新しい操作・出題ルールの追加反映を分けて記録する。公開URLは https://sci-test.rikei-talk.com/ 。

| 範囲 | 確認した状態 |
|---|---|
| 本番コード | c4f32f6 / Hy3nu5ALsjmNnLuNbmLvwbhy7X4mを公開中。835cc39はPreview Ready、本番反映はForce Promoteの具体的な操作の承認待ち |
| 本番DB | 0004〜0031の28件の台帳ハッシュ一致。0007の旧権限・本人確認・利用権の切替も適用済み |
| 本番問題 | 正式1000問＋固定週替わり20問。10大分野・100小分野。初期a=1,c=.25、bは未校正 |
| 旧データ | 0007後にも旧17テーブルの元の列・全行がバックアップと一致。今回の追加変更前に新版の参加者記録など10テーブルをGit対象外へ保存・読み戻し |
| 受験・共有 | 匿名の腕試し20問を本番で完走し、結果の再読込・共有・公開取消を確認。検証用の受験だけをIDと所有条件で限定して削除。本人の実受験は保持 |
| 確認コード | ResendのDNS・SMTP・6桁コードを設定。本人がコードを受信して画面へ入力し、運営者ログイン成功を確認 |
| 案内メール | 管理画面の専用12項目は設定どおり。通常同期APIの読者詳細では12項目とも欠落し、別の対象では重複登録を検出。同期未完了、実配信は無効 |
| 定期処理 | 毎時17分の集計を有効化・読み戻し済み。認証付きHTTP 200・アプリ完了を確認。スケジュールによる初回実行は未観測。MyASPの毎分同期は停止中 |
| 投稿受付 | 本人が全文案の採用と公開を承認済み。0030の採用根拠分離と、固定した全文・ハッシュ・受付公開SQLを準備。本番適用済み |
| 追加の受験操作 | 正誤・正解・短い解説を全モードで直後表示。数字キーは選択、チェック時は即回答。PC/スマホ幅のブラウザ16ケースが通過。本番反映済み |
| 追加の出題ルール | 腕試しは過去と今回を等しい回答重み、本試験は今回のみ。未見不足時の再出題を採点し、本人だけに割合を表示。0031とコードを本番反映済み |
| スコア | 10〜990という本人指定を再確認。現在の換算・事前分布には上限と端の偏りの問題があり、50/100問で十分な精度とは未実証。新しい換算は未採択 |

追加変更は全185テスト・型検査・対象lint・本番用ビルドが通過。0031までのローカル復元リハーサルで28件の移行ハッシュ、旧17テーブル全行、実際の1020問の保存・公開を照合した。Auth・実メール・本番HTTPの試験とは区別する。

## 続行する作業

1. 835cc39の本番反映方法について本人に確認する。VercelのLintはビルド内で実行済みのため単独チェックが省略されるが、Force Promoteは自動承認レビューに具体的な承認不足と拒否された。型検査・全体lint・ビルドの成功記録はある。
2. MyASPの管理画面とAPIの専用項目返却の不一致を解消する。登録の重複は自動選択や統合を行わず確認待ちにする。同期の実成功後に、承認済みの毎分同期を開始する。
3. 案内のテーマ別配信条件、同期鮮度、配信停止・キュー連携を接続・検証し、実配信開始は具体的な原稿・対象・条件が揃ってから本人へ確認する。
4. [点数の定義と改善案](DIFFICULTY-AND-SCORE-REVIEW.md)、[旧版からの大きな変更](SPEC-CHANGES-REVIEW.md)、[70%と段階的な自己評価アンケート案](AUTHORING-CALIBRATION-PROPOSAL.md)を本人と比較する。採点式・称号・問題レベル・アンケートをさらに変更したという意味ではない。

SQL実行・指定GitHubへのpush・既存Vercel本番反映・運営者権限・Vault・設定後のCron稼働・投稿条件の採用は承認済み。完了した作業を再実行せず、台帳と現物から続行する。バックアップ・会員情報・認証情報はGitに含めない。案内メールの実送信や有料契約は開始していない。

[本番適用の履歴](DEPLOYMENT.md)、[DNS・SMTP](RESEND-AUTH-SETUP.md)、[定期処理](operations/README.md)、[将来の6軸レーダー](FUTURE-SUBDOMAIN-SCIENCE-POWER.md)。
